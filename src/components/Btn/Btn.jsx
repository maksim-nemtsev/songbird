import React from 'react';
import './Btn.css';
import { Button } from 'react-bootstrap';

const Btn = () => {
  return (
    <Button type="button" className="btn">
      Next
    </Button>
  );
};

export default Btn;
